# Run v25.9.16.7.2.64.16.5.7.2.16

## Trước khi build

```bash
python scripts/version-contract-check.py
cd backend
pytest -q app/tests/test_v25_9_16_7_2_64_16_5_7_2_16_ap_sync_selected_subject_scope.py
```

## Sau khi deploy

1. Vào `/subject-management`, chọn học kỳ và gán một số môn thành CMS/Udemy; để lại ít nhất một môn `Chưa chọn` để test âm.
2. Vào `/ap-sync`, chọn cùng học kỳ.
3. Xác nhận KPI `Môn được đồng bộ` bằng đúng số môn CMS + Udemy hợp lệ, không phải tổng catalog.
4. Chạy `Chỉ kiểm tra kế hoạch` trước. `plan.subjects_by_campus[*].preview` chỉ chứa các mã đã chọn.
5. Chạy đồng bộ thật và kiểm tra progress `/get-data-cms` chỉ xuất hiện với các mã đã chọn.
6. Bỏ chọn một môn, làm mới `/ap-sync`; môn đó phải biến khỏi phạm vi.

Không cần migration mới cho release này.
