# v25.9.15.6.31.4 - Bank Hierarchy Polish + Stronger Actions + Question Card Typography

## Nội dung

- Bỏ thanh bước `1. Bộ môn → 2. Môn → 3. Version → 4. Bài → 5. Câu hỏi → 6. Release` khỏi các trang Bank vì breadcrumb đã thể hiện đầy đủ ngữ cảnh.
- Làm button nổi bật hơn, dễ thấy hơn trong danh sách, popup và workspace.
- Tinh chỉnh sidebar nhóm Vận hành theo luồng ngân hàng đề mới.
- Làm thẻ câu hỏi dễ đọc hơn: font lớn hơn, spacing tốt hơn, đáp án rõ hơn, action dễ bấm hơn.
- Giữ breadcrumb: `Bộ môn › Môn › Version › Bài` là đường dẫn chính.

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build frontend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate frontend
```

Không cần migrate DB.
