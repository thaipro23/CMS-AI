# v25.9.16.2.17 — Rebased Academic AP Sync on latest 38.8.4 UI

Bản này lấy zip gốc mới nhất `v25.9.15.6.38.8.4-search-index-drilldown-performance-guard` làm nền, rồi áp lại các chức năng Academic/AP:

- `/premises`
- `/semesters`
- `/ap-sync`
- `/student-management`
- API `/api/academic/*`
- migrations Academic/AP `0027_v25_9_16_0...` đến `0039_v25_9_16_2_13_terms`

Điểm chính: giữ nguyên CSS/layout/product shell mới của `38.8.4`; không dùng lại `globals.css`/`AppShell` cũ từ các zip Academic trước.

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend   alembic -c alembic.ini upgrade head

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

## Kiểm tra

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend   alembic -c alembic.ini heads

docker logs ai-backend --tail=200
```

Mở:

- `/premises`
- `/semesters`
- `/ap-sync`
- `/student-management`
