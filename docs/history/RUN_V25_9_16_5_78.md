# v25.9.16.5.78 — Enterprise UX/UI Polish

## Mục tiêu
- Áp dụng hướng thiết kế dashboard/admin hiện đại: giảm nhiễu thị giác, rõ phân cấp, bảng dễ quét, loading không làm người dùng tưởng hệ thống treo.
- Ưu tiên màn `/teacher-management` vì đây là màn vận hành dữ liệu lớn, cần nhẹ và dùng hằng ngày.

## Thay đổi chính
- Làm lại hero/header của `/teacher-management` thành khối điều hành rõ phạm vi: cơ sở, học kỳ, trang hiện tại.
- Thêm note vận hành: mặc định 50 giảng viên/trang, mở chi tiết mới tải lớp, báo cáo toàn hệ dùng Excel/job nền.
- Re-style filter bar thành sticky command bar, giúp lọc không bị mất khi cuộn bảng dài.
- Re-style KPI cards: rõ nhãn, số chính, chú thích; giảm cảm giác quá nhiều số cạnh tranh nhau.
- Re-style table: sticky header, hover row, expanded row, avatar giảng viên, action rõ hơn.
- Loading dùng skeleton row thay vì dòng text đơn giản.
- Empty state có hướng dẫn và nút xóa bộ lọc nhanh.
- Global UI polish: focus-visible, button/input state, card/surface, sidebar active state, reduced-motion.

## File chính
- `frontend/app/teacher-management/page.tsx`
- `frontend/app/globals.css`
- `frontend/components/layout/AppShell.tsx`

## Kiểm tra
- Backend compile: pass.
- Frontend typecheck: pass.
- Training policy tests: `11 passed`.
- Next production build: compiled successfully, sau đó sandbox bị `EPIPE` tại bước `Collecting page data`; chưa xác nhận full build hoàn tất trong sandbox.

## Deploy
```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build frontend backend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate frontend backend
```
