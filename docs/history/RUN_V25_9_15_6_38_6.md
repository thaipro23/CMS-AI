# v25.9.15.6.38.6 - Frontend Operations Polish + Review Metadata + Ratio Zero Fix

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic heads

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

Set:

```env
APP_VERSION=25.9.15.6.38.6-frontend-ops-polish-ratio-fix
```

## Test checklist

- `/audit` shows modern timeline and does not leak out-of-scope audit rows.
- `/jobs` shows operation jobs, legacy generation jobs, recent quizzes, and ops activity.
- `/bank/history` shows release history, quiz history, rollback, and event timeline.
- `/users` no longer contains verbose product/architecture copy; scope dropdown has a search box.
- `/bank` daily trend chart shows y-axis counts.
- Clicking dashboard KPI approved/rejected shows all scoped questions, not only the current actor's actions, and shows reviewer/rejection note when available.
- Generate with EASY=100, MEDIUM=0, HARD=0 succeeds; 0 values are no longer replaced by defaults.
