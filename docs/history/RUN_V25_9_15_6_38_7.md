# RUN v25.9.15.6.38.7 - Frontend Popup Table IA Polish

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic heads

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

## ENV

```env
APP_VERSION=25.9.15.6.38.7-frontend-popup-table-ia-polish
```

## Verify UI

- `/bank`: Alerts and Recent Activity are popup buttons beside Quick Search.
- `/bank/history`: Quiz Open edX and Recent Releases are popup buttons; no event timeline.
- `/jobs`: no legacy Generation jobs; Operation jobs is a table; recent activity/quiz are popups.
- `/audit`: log is table-based.
- `/users`: Excel import and current permissions are popups; activity log section removed.
