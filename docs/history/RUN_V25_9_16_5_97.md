# v25.9.16.5.97 — Connector Sync Contract Lock

## Mục tiêu

Ngăn lỗi Course completion quay lại: N/A sai, 21.4% do itembank/problem, hoặc ghi đè snapshot đúng bằng payload rỗng.

## Contract mới

Connector trả bắt buộc:

```json
{
  "connector_version": "25.9.16.5.98",
  "connector_contract_version": "learning-sync/v25.9.16.5.98",
  "progress_contract": {
    "completion_source": "StudentModuleSequentialFallback",
    "denominator": "reachable_sequential_subsections",
    "numerator": "studentmodule_sequential_position_rows",
    "ignored_studentmodule_types": ["itembank", "problem", "video", "vertical", "chapter", "course"],
    "zero_progress_rule": "students_with_only_itembank_rows_return_0_over_total_sequential"
  }
}
```

## Backend guard

`sync_class_learning_insight()` gọi:

```python
self._validate_connector_learning_contract(analytics_payload, course_id=course_id)
```

Nếu connector thiếu version/contract, backend dừng sync và không ghi snapshot.

## Không ghi đè dữ liệu tốt bằng null

`_upsert_learning_snapshot()` giữ lại giá trị cũ nếu payload mới không có progress contract hợp lệ hoặc thiếu grade/progress:

```text
previous_progress_percent
previous_grade_percent
previous_completed_blocks
previous_total_blocks
```

## Test

```bash
cd /opt/ai-server
DATABASE_URL=sqlite+pysqlite:///:memory: pytest -q \
  backend/app/tests/test_v25_9_16_5_98_connector_contract_and_cache.py \
  backend/app/tests/test_v25_9_16_5_85_learning_sync_hardening.py
```
