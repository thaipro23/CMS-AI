# RUN v25.9.15.6.38.5.1 — Next Suspense Build Hotfix

## Mục tiêu

Sửa lỗi Docker frontend build:

```text
useSearchParams() should be wrapped in a suspense boundary at page "/bank"
useSearchParams() should be wrapped in a suspense boundary at page "/bank/search"
```

## Env

```env
APP_VERSION=25.9.15.6.38.5.1-next-suspense-build-hotfix
```

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic heads

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

## Test

```bash
curl -I http://ai.cms-test.poly.edu.vn/bank
curl -I http://ai.cms-test.poly.edu.vn/bank/search
```

Mở UI:

```text
http://ai.cms-test.poly.edu.vn/bank
http://ai.cms-test.poly.edu.vn/bank/search?entity=questions&status=pending_review
http://ai.cms-test.poly.edu.vn/users
```
