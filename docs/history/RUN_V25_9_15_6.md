# RUN v25.9.15.6

Bản này sửa đúng nghiệp vụ clone version môn:

```text
Clone = copy bản làm việc sang kỳ mới
Release = chốt tay sau khi sửa xong
Diff = chỉ chạy khi tài liệu kỳ mới bị đổi
```

## Build

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production build --no-cache backend worker frontend
```

## Migration

Bản này không thêm migration mới, vẫn chạy lệnh chuẩn để đảm bảo DB ở revision mới nhất:

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head
```

## Up service

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

## Check nhanh

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production ps
docker compose -f docker-compose.prod.yml --env-file .env.production logs backend --tail=200
curl -i http://localhost:8000/api/health
```

## Test clone đúng nghiệp vụ

```text
1. Vào /bank
2. Clone DOM123_SP25 sang DOM123_SU25
3. Kiểm tra SU25 có bài/tài liệu/câu hỏi approved mới
4. Kiểm tra SU25 chưa có Release
5. Upload tài liệu mới vào SU25
6. UI hiện cảnh báo cần kiểm tra khác biệt
7. Bấm Kiểm tra khác biệt
8. Chỉ sau khi sửa xong mới bấm Tạo Release/Publish Library
```
