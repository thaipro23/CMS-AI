# v25.9.15.6.29 - Modern White UI Refresh

## Mục tiêu

- Bỏ toàn bộ cam nhạt làm màu nền/chủ đạo.
- Chuyển AI Server sang giao diện trắng, hiện đại, dễ đọc hơn.
- Giữ accent xanh nhẹ cho trạng thái tương tác, nút chính và active menu.
- Không đổi logic backend/database.

## File chính

- `frontend/app/globals.css`

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build frontend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate frontend
```

Sau đó vào `http://ai.cms-test.poly.edu.vn/bank/quiz` và Ctrl+F5.
