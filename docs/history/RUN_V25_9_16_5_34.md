# v25.9.16.5.34 — Verified Cockpit Token Rebuild

## Mục tiêu

Bản này không vá chồng UI nữa. Mục tiêu là kiểm tra lại frontend và áp bộ token màu/font/bo góc/spacing đã chốt vào shell chung của AI Server.

## Đã áp dụng

### Token màu/font/bo góc

```css
--bg-base: #06050A;
--bg-surface: #19182A;
--border-subtle: #2B2A39;
--text-primary: #F1F0FA;
--text-secondary: #B7B5C9;
--text-muted: #8B899C;
--accent-primary: #6366F1;
--accent-primary-light: #A78BFA;
--accent-info: #3B82F6;
--accent-success: #10B981;
--accent-warning: #FBBF24;
--accent-danger: #EF4444;
--accent-pink: #EC4899;
--accent-cyan: #22D3EE;
--radius-card: 16px;
--radius-md: 10px;
--radius-sm: 4px;
--radius-full: 9999px;
--font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

### Shell/layout

- Sidebar rộng 292px.
- Nav item cao 44px.
- Card/panel dùng `--bg-surface`, `--border-subtle`, `--radius-card`.
- Không dùng shadow đổ rõ; depth tạo bằng surface layer + hairline border.
- Active/hover dùng accent opacity thấp.
- Icon sidebar chuyển sang SVG stroke-based inline.
- Toggle sáng/tối ở top-right, lưu `ai-server-theme`.

### Dashboard/charts

- Gỡ hardcoded chart colors trong `BankDashboardPage.tsx`.
- Donut/line/gridline dùng CSS variables.
- Semantic colors map theo token.

## Các lệnh đã chạy trong sandbox

```bash
cd /mnt/data/work_v34/frontend
npm run typecheck
```

Kết quả: OK.

```bash
cd /mnt/data/work_v34
python3 -m compileall -q backend/app backend/alembic/versions openedx-connector-plugin/openedx_ai_connector openedx-unit-reset-plugin/openedx_unit_reset
```

Kết quả: OK.

```bash
cd /mnt/data/work_v34/frontend
NEXT_TELEMETRY_DISABLED=1 npm run dev -- -p 3100
curl -I --max-time 10 http://127.0.0.1:3100/bank
```

Kết quả: HTTP 200 OK.

## Kiểm tra grep đã chạy

```bash
grep -RInE "#[0-9a-fA-F]{3,8}" frontend/app frontend/components frontend/context frontend/lib frontend/types --exclude-dir=node_modules --exclude-dir=.next | grep -v 'frontend/app/globals.css'
```

Kết quả: không còn hardcoded hex ngoài `globals.css`.

```bash
grep -RIn "box-shadow" frontend/app frontend/components --exclude-dir=node_modules --exclude-dir=.next
```

Kết quả: không còn `box-shadow` trong app/components.

## Lưu ý trung thực

Trong sandbox, `next build` bị timeout/EPIPE tại worker static/page-data stage dù `typecheck` pass và dev server render `/bank` được HTTP 200. Vì vậy không ghi "npm build pass" giả. Trên UAT cần chạy Docker build thật.

## Deploy UAT

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build frontend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate frontend
```

Deploy full nếu cần:

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```
