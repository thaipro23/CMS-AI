# v25.9.16.7.2.14 — Management KPI Scope Clarity

Fixes misleading KPI scope between `/student-management` and `/teacher-management`.

- `/student-management` now labels subject-page KPIs as current-page counters.
- `/teacher-management` now labels class/student counters as teacher-assignment workload counters.
- Teacher report cache notice now states when KPIs are full filtered cache versus dynamic/page scope.
- CMS sync pill in teacher rows is no longer always green when value is `0/total`.

No migration. No DB reset.
