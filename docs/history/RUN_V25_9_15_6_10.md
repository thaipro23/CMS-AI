# Run v25.9.15.6.10

Bản này sửa cả backend và frontend. Chạy lại:

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build --no-cache backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

Xem log realtime backend:

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production logs backend -f --tail=300
```

Test UI:

```text
/bank
/bank/departments
/bank/departments/{departmentId}/subjects
/bank/subjects/{subjectId}/versions
/bank/subject-versions/{versionId}/chapters
/bank/chapters/{chapterId}
```

Kiểm tra:

```text
- F5 không còn 401 Unauthorized.
- /bank không gọi dashboard/overview 2 lần trong môi trường production.
- Số còn việc = những nơi thực sự còn câu chờ duyệt hoặc câu lỗi.
```
