# v25.9.16.5.79 — Enterprise UX/UI Rebuild Pass

## Mục tiêu

Bản này tiếp tục từ v25.9.16.5.78, tập trung làm lại UX/UI theo hướng enterprise admin console cho hệ thống AI Server / Open edX CMS.

Hướng thiết kế đã áp dụng:
- Design-system/local-first: gom toàn bộ màu, surface, border, radius, shadow, focus vào token CSS để sửa toàn hệ từ một nguồn.
- Dashboard/data-table-first: ưu tiên bảng dữ liệu lớn, sticky filter, sticky table header, row hover, zebra scanning, KPI card thống nhất.
- Empty/loading states: dùng skeleton và empty state có hướng dẫn thao tác thay vì chỉ hiện dòng text hoặc spinner.
- Accessibility: focus-visible rõ, skip-link, giảm motion theo `prefers-reduced-motion`, target button/input nhất quán.
- Enterprise visual: nền sáng, ít nhiễu, sidebar nhẹ, active state rõ, card phẳng, status pill rõ màu.

## File đã sửa

### Frontend shell
- `frontend/components/layout/AppShell.tsx`
  - Đổi shell sang kiểu product command center.
  - Thêm section label trên topbar theo nhóm chức năng: Ngân hàng câu hỏi / Vận hành đào tạo / Quản trị hệ thống.
  - Thêm mô tả route ở topbar.
  - Thêm session pill và role pill ở topbar.
  - Thêm skeleton nav khi quyền chưa sẵn sàng.
  - Update footer version lên `v25.9.16.5.79`.

### Global UX/UI
- `frontend/app/globals.css`
  - Thêm bộ token `--od-*` cho v79.
  - Rebuild sidebar, topbar, content shell, card, button, input, filter bar, KPI card, table, empty state, modal, status pill.
  - Áp dụng responsive 1200px/760px.
  - Thêm skeleton loading và reduced-motion.
  - Mở rộng polish sang các màn có class hiện hữu: `/bank`, `/teacher-management`, `/student-management`, `/jobs`, `/audit`, `/ap-sync`, `/premises`, `/semesters`, `/users`, `/settings`.

### Teacher Management
- `frontend/app/teacher-management/page.tsx`
  - Thêm `teacher-quality-grid` mô tả nguyên tắc vận hành báo cáo: dữ liệu theo phạm vi, drill-down thay vì quét nặng, cảnh báo không đếm trùng.
  - Giữ nguyên logic tải 50 GV/page, expand lớp, export Excel.

### Student Management
- `frontend/app/student-management/page.tsx`
  - Đổi loading danh sách môn từ text sang skeleton rows.
  - Đổi empty state thành hướng dẫn có nút “Xóa bộ lọc nhanh”.

### Version
- `frontend/package.json`
- `frontend/package-lock.json`
  - Update version frontend lên `25.9.16.5.79`.

## Kiểm tra trong sandbox

Đã chạy:

```bash
npm --prefix frontend run typecheck
python3 -m compileall -q backend/app
```

Kết quả:
- TypeScript typecheck: PASS.
- Backend Python compile: PASS.

Đã thử:

```bash
npm --prefix frontend run build
```

Kết quả sandbox:
- Next compiled successfully.
- Linting/type validity passed.
- Static pages generated 27/27.
- Sandbox bị timeout ở bước `Collecting build traces`, giống dạng treo/timeout môi trường build sandbox. Khi build bằng Docker server thật cần xem tiếp log frontend container/build.

## Deploy

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build frontend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate frontend
```

Nếu muốn build lại full backend/worker/frontend:

```bash
cd /opt/ai-server

docker compose -f docker-compose.prod.yml --env-file .env.production build backend worker frontend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

## Kiểm tra sau deploy

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -n 200 frontend
curl -i http://127.0.0.1:3000/teacher-management
```

Màn nên kiểm tra thủ công:
- `/teacher-management`
- `/student-management`
- `/jobs`
- `/ap-sync`
- `/bank`
- `/semesters`
- `/users`

## Ghi chú

Bản này không đổi nghiệp vụ backend/RBAC/API. Đây là bản UX/UI rebuild pass để làm nền trước khi tiếp tục các kế hoạch kế tiếp như cache summary, Excel background job, Assignment defense workflow và Final test rule.
