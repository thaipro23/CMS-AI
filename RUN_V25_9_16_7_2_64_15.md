# Chạy v25.9.16.7.2.64.15

Artifact:

```text
ai-server-openedx-v25.9.16.7.2.64.15-scoped-rbac-analytics-table-contract.zip
```

Root trong zip:

```text
ai_server_openedx_v25_9_16_7_2_64_15
```

## 1. Giải nén và đồng bộ source

```bash
cd /opt/ai-server

unzip -o ai-server-openedx-v25.9.16.7.2.64.15-scoped-rbac-analytics-table-contract.zip \
  -d /tmp/ai-server-v25.9.16.7.2.64.15

rsync -a --delete \
  /tmp/ai-server-v25.9.16.7.2.64.15/ai_server_openedx_v25_9_16_7_2_64_15/ \
  /opt/ai-server/
```

Không dùng `docker compose down -v`, không xóa volume và không sửa tay `alembic_version`.

## 2. Cập nhật version

Trong `.env.production`:

```env
APP_VERSION=25.9.16.7.2.64.15
NEXT_PUBLIC_APP_VERSION=25.9.16.7.2.64.15
```

UAT nội bộ đang dùng HTTP qua file hosts phải giữ:

```env
APP_ENV=uat
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAMESITE=lax
DB_STATEMENT_TIMEOUT_MS=5000
```

Chỉ bật `AUTH_COOKIE_SECURE=true` sau khi endpoint thực sự chạy HTTPS.

## 3. Kiểm tra migration và index hiện hữu

Bản này không có migration mới:

```bash
docker compose -f docker-compose.prod.yml \
  --env-file .env.production \
  run --rm backend alembic -c alembic.ini current
```

Kết quả mong muốn:

```text
0052_v25_9_16_7_2_27_learning_behavior_logic_calibration
```

Kiểm tra index, không tạo thủ công:

```bash
docker compose -f docker-compose.prod.yml \
  --env-file .env.production \
  exec postgres sh -lc '
psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "
SELECT indexname, indexdef
FROM pg_indexes
WHERE schemaname = '\''public'\''
  AND tablename = '\''academic_classes'\''
  AND indexname = '\''ix_academic_classes_scope_lookup'\'';
"'
```

## 4. Build và triển khai

```bash
docker compose -f docker-compose.prod.yml \
  --env-file .env.production \
  build backend frontend worker beat

docker compose -f docker-compose.prod.yml \
  --env-file .env.production \
  up -d --force-recreate backend frontend worker beat
```

## 5. Smoke test bắt buộc

```bash
./scripts/uat-runtime-verify.sh
./scripts/uat-build-gate.sh
```

Kiểm tra bằng trình duyệt:

1. `/analytics/learning`: tab Network không còn tự gọi pilot/evidence/release/operations khi chỉ xem kết quả lớp.
2. Người không có `ops.readiness.view` không thấy nút mở kiểm tra vận hành.
3. STT và cột nhận diện đầu tiên liền nhau, không có khoảng trống do sticky offset cũ.
4. Admin thấy toàn bộ menu/nút.
5. Trưởng bộ môn chỉ thao tác trong bộ môn và nhánh dưới.
6. Chủ môn chỉ thao tác trong môn và nhánh dưới.
7. Người duyệt chỉ thấy thao tác được cấp trong phạm vi được gán.
8. Chủ cơ sở chỉ thấy lớp thuộc cơ sở.
9. Giảng viên chỉ thấy lớp được AP phân công và không thấy nút vận hành/recalculate.
10. Trang chi tiết lớp không còn panel `Kiểm tra identity CMS/RollNumber`.
11. `/api/health/uat-ux-acceptance` không còn 23 blocker do module missing.
12. `/api/health/security-attack-simulation` nhận đủ 20 source controls thay vì 0 protected.
13. Maintainability không còn file `0 dòng`; cảnh báo large-file kế thừa được hiển thị đúng.

## 6. Rollback source

Rollback bằng artifact `.64.14`, không rollback database vì `.64.15` không có migration mới.
