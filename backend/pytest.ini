[pytest]
pythonpath = .
testpaths = app/tests
markers =
    integration: requires disposable PostgreSQL and Redis services
    current_contract: validates the active release contract
filterwarnings =
    ignore::DeprecationWarning
