# Run v25.9.16.7.2.64.16.5.7.2.1

## 1. Pull source and verify npm registry

```bash
cd /opt/ai-server
git pull
chmod +x scripts/npm-public-registry-lockfile-report.sh
./scripts/npm-public-registry-lockfile-report.sh
```

Expected:

```text
status: READY
blocked_count: 0
frontend wrong_registry_count: 0
e2e wrong_registry_count: 0
```

A direct grep must return no result:

```bash
grep -R "applied-caas-gateway\|internal.api.openai.org\|artifactory/api/npm" \
  frontend/package-lock.json e2e/package-lock.json frontend/.npmrc e2e/.npmrc
```

## 2. Build

Do **not** use `--no-cache` for normal deployments.

```bash
docker compose \
  -f docker-compose.prod.yml \
  --env-file .env.production \
  build frontend backend worker worker-heavy worker-analytics beat runtime-init migrate
```

Both Docker and lockfiles force downloads through:

```text
https://registry.npmjs.org/
```

## 3. Migration

No new migration is included. Verify the canonical migration service:

```bash
docker compose \
  -f docker-compose.prod.yml \
  --env-file .env.production \
  run --rm migrate
```

Alembic head remains:

```text
0053_v25_9_16_7_2_64_16_5_4_diff_idempotency.py
```

## 4. Start without rebuilding

```bash
docker compose \
  -f docker-compose.prod.yml \
  --env-file .env.production \
  up -d --no-build --force-recreate \
  frontend backend worker worker-heavy worker-analytics beat
```

## 5. UAT HTTP settings

For the isolated internal HTTP UAT:

```env
APP_ENV=uat
AUTH_COOKIE_SECURE=false
ALLOW_INSECURE_UAT_HTTP=true
AUTH_COOKIE_SAMESITE=lax
```

Production over HTTPS must use:

```env
APP_ENV=production
AUTH_COOKIE_SECURE=true
ALLOW_INSECURE_UAT_HTTP=false
```
