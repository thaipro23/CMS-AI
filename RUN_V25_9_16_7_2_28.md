# v25.9.16.7.2.28 — Bank Quiz Status Selector Polish

## Mục tiêu

Sửa UX trang `/bank/quiz` theo yêu cầu vận hành: bỏ cột/nhãn `Tạo gì`; đưa lựa chọn tạo bài kiểm tra vào cột `Trạng thái` với đúng 3 lựa chọn rõ nghĩa.

## Thay đổi chính

- Bảng map Quiz không còn cột `Tạo gì`.
- Cột `Trạng thái` chứa dropdown lựa chọn:
  - `Tạo Quiz`
  - `Tạo Final test`
  - `Không tạo`
- Bỏ lựa chọn `Assignment/nội dung` khỏi UI trang `/bank/quiz`.
- Các dòng có tiêu đề Final test mặc định chọn `Tạo Final test`.
- Các dòng Assignment/ASM mặc định chọn `Không tạo`.
- Backend label preview/apply cũng đổi về `Không tạo`, không còn text `Không tạo quiz` hoặc `Assignment/nội dung` trong luồng này.

## Deploy

```bash
cd /opt/ai-server

unzip -o ai-server-openedx-v25.9.16.7.2.28-bank-quiz-status-selector-polish.zip -d /tmp/ai-server-v25.9.16.7.2.28

rsync -a --delete /tmp/ai-server-v25.9.16.7.2.28/ai_server_openedx_v25_9_16_7_2_28/ /opt/ai-server/

docker compose -f docker-compose.prod.yml --env-file .env.production build backend frontend

docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend frontend
```

Không có migration mới trong bản này. Nếu server chưa chạy migration của `.27`, vẫn cần `alembic upgrade head` theo bản `.27` trước đó.

## Env

```env
APP_VERSION=25.9.16.7.2.28
NEXT_PUBLIC_APP_VERSION=25.9.16.7.2.28
```

## Kiểm tra nhanh

Vào `/bank/quiz`:

1. Nhập Khóa học ID.
2. Chạy kiểm tra map.
3. Bảng kết quả phải chỉ còn cột `Trạng thái`.
4. Dropdown trong cột `Trạng thái` chỉ có: `Tạo Quiz`, `Tạo Final test`, `Không tạo`.
5. Không còn text `Tạo gì`, `Assignment/nội dung`, `Không tạo quiz`.
