# Deploy v25.9.16.7.2.64.16.5.7.2.9 — Batch 35.3.2

No Alembic migration is required. Existing head remains `0057_v25_9_16_7_2_64_35`.

Update production version:

```bash
sed -i 's/^APP_VERSION=.*/APP_VERSION=25.9.16.7.2.64.16.5.7.2.9/' .env.production
```

Build the affected services:

```bash
docker compose --env-file .env.production -f docker-compose.prod.yml build backend frontend worker worker-heavy
```

Recreate them:

```bash
docker compose --env-file .env.production -f docker-compose.prod.yml up -d --no-deps --force-recreate backend frontend worker worker-heavy
```

Then inspect runtime logs:

```bash
docker compose --env-file .env.production -f docker-compose.prod.yml logs --tail=300 backend worker worker-heavy frontend
```

Acceptance: import one current Udemy export containing `ID bên ngoài` as `.csv` and one `.xlsx`; both must resolve Email/progress without removing or moving columns.
