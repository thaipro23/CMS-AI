# Deploy v25.9.16.7.2.64.16.2 — Production

Artifact:

```text
ai-server-openedx-v25.9.16.7.2.64.16.2-bank-review-quiz-workbench.zip
```

Root:

```text
ai_server_openedx_v25_9_16_7_2_64_16_2
```

Bản này tiếp tục trực tiếp từ `.64.16.1`. Không có migration mới; migration cuối vẫn là `0052_v25_9_16_7_2_27_learning_behavior_logic_calibration.py`.

## 1. Sao lưu cấu hình và xác minh migration hiện tại

```bash
cd /opt/ai-server
cp .env.production ".env.production.backup.$(date +%Y%m%d-%H%M%S)"

docker compose -f docker-compose.prod.yml --env-file .env.production exec backend \
  alembic -c alembic.ini current
```

Không reset database, không xóa volume, không sửa tay `alembic_version`, không chạy `docker compose down -v`.

## 2. Giải nén và đồng bộ source

```bash
rm -rf /tmp/ai-server-v25.9.16.7.2.64.16.2
mkdir -p /tmp/ai-server-v25.9.16.7.2.64.16.2

unzip -q ai-server-openedx-v25.9.16.7.2.64.16.2-bank-review-quiz-workbench.zip \
  -d /tmp/ai-server-v25.9.16.7.2.64.16.2

rsync -a --delete \
  /tmp/ai-server-v25.9.16.7.2.64.16.2/ai_server_openedx_v25_9_16_7_2_64_16_2/ \
  /opt/ai-server/
```

## 3. Cấu hình production

```env
APP_ENV=production
APP_VERSION=25.9.16.7.2.64.16.2
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_APP_VERSION=25.9.16.7.2.64.16.2
NEXT_PUBLIC_ENABLE_DIAGNOSTICS_UI=false

AUTH_MODE=openedx_sso
ALLOW_DEMO_ROLE_HEADER=false
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAMESITE=lax
DEBUG=false
AUTO_CREATE_TABLES=false
DB_STATEMENT_TIMEOUT_MS=5000
```

CORS production chỉ giữ origin HTTPS đã phê duyệt. Sidebar luôn tối; topbar và vùng làm việc luôn sáng.

## 4. Build và triển khai

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production \
  build backend worker beat frontend

docker compose -f docker-compose.prod.yml --env-file .env.production \
  up -d --force-recreate backend worker beat frontend
```

## 5. Kiểm tra kỹ thuật

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production ps

docker compose -f docker-compose.prod.yml --env-file .env.production \
  logs --since=10m backend worker beat frontend

curl -fsS https://api-ai.cms-test.poly.edu.vn/api/health
```

Kiểm tra phiên bản thực tế:

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production exec backend \
  python -c "from app.core.config import settings; print(settings.app_version)"
```

Kết quả phải là `25.9.16.7.2.64.16.2`.

## 6. Browser UAT bắt buộc

```text
- Sidebar tối; topbar/content sáng; không có theme switcher.
- Bank Dashboard compact, không còn hero/card trang trí chiếm chiều cao.
- Bộ môn → Môn → Phiên bản → Bài giữ đúng breadcrumb và scope RBAC.
- Bảng hierarchy ưu tiên cột tên/trạng thái/thao tác; cột số và cột phụ không chiếm chiều rộng thừa.
- Trang Bài mở câu hỏi theo preview-first; duyệt/từ chối/sửa thực hiện trong preview drawer.
- Action trên từng dòng câu hỏi không còn bốn nút cạnh nhau.
- Bulk action chỉ hiện khi đã chọn câu và phân biệt chọn trang hiện tại/toàn bộ kết quả lọc.
- /bank/quiz hiển thị workflow ba bước toàn chiều rộng, mapping Section/Release dễ đọc và không có panel phải làm hẹp bảng.
- Lịch sử Quiz/Release dùng tab và giữ tab/filter trong URL sau F5/Back.
- Kiểm tra create Quiz/Final test, rollback và frozen Release membership bằng dữ liệu UAT thật.
- Kiểm tra SYSTEM_ADMIN, DEPARTMENT_HEAD, SUBJECT_OWNER và QUESTION_REVIEWER bằng tài khoản thật.
- /ops/readiness và các nút test/mock/doctor không xuất hiện trong frontend production.
```

## 7. Rollback

Rollback source về artifact `.64.16.1`. Không rollback database vì `.64.16.2` không có migration mới.
