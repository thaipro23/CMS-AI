# Deploy v25.9.16.7.2.64.16.5.7.2.10 — Batch 35.3.3

No Alembic migration is required. Existing head remains `0057_v25_9_16_7_2_64_35`.

Update production version:

```bash
sed -i 's/^APP_VERSION=.*/APP_VERSION=25.9.16.7.2.64.16.5.7.2.10/' .env.production
```

Build the affected services:

```bash
docker compose --env-file .env.production -f docker-compose.prod.yml build backend frontend worker worker-heavy
```

Recreate them:

```bash
docker compose --env-file .env.production -f docker-compose.prod.yml up -d --no-deps --force-recreate backend frontend worker worker-heavy
```

Inspect runtime logs:

```bash
docker compose --env-file .env.production -f docker-compose.prod.yml logs --tail=500 backend worker worker-heavy frontend
```

## Acceptance

1. Open `Vận hành đào tạo > Quản lý sinh viên Udemy`.
2. Choose System + Term, click `Import hàng loạt Udemy`.
3. Choose the target Block.
4. Upload multiple `.csv/.xlsx` files or one `.zip` containing up to 50 Udemy reports.
5. Confirm valid files are queued as one persistent parent job.
6. Confirm duplicate files are skipped without duplicating progress snapshots.
7. Confirm files from another term/branch, a filename/report subject mismatch, unsupported members, or non-Udemy subjects are rejected per file while valid siblings continue.
8. F5 or navigate away and verify the Celery job continues in `/jobs`.

For the supplied `Udemy_11.8.zip`, preflight against Summer 2026 must detect 38 reports: 35 candidates, 2 Spring 2026 term mismatches, and 1 `DAT115` filename vs `DAT110` path-title mismatch before DB mapping is considered.
