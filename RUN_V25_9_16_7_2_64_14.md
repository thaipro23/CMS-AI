# Chạy v25.9.16.7.2.64.14

Artifact:

```text
ai-server-openedx-v25.9.16.7.2.64.14-training-ops-ux-uat-gate.zip
root: ai_server_openedx_v25_9_16_7_2_64_14
```

Không có migration mới. Không reset DB, không xóa volume, không sửa tay `alembic_version`.

## Deploy

```bash
cd /opt/ai-server

unzip -o ai-server-openedx-v25.9.16.7.2.64.14-training-ops-ux-uat-gate.zip \
  -d /tmp/ai-server-v25.9.16.7.2.64.14

rsync -a --delete \
  /tmp/ai-server-v25.9.16.7.2.64.14/ai_server_openedx_v25_9_16_7_2_64_14/ \
  /opt/ai-server/

docker compose -f docker-compose.prod.yml --env-file .env.production \
  build backend frontend worker beat

docker compose -f docker-compose.prod.yml --env-file .env.production \
  up -d --force-recreate backend frontend worker beat
```

## Version env

```env
APP_VERSION=25.9.16.7.2.64.14
NEXT_PUBLIC_APP_VERSION=25.9.16.7.2.64.14
```

## Verify nhanh

```bash
curl -fsS https://api-ai.cms-test.poly.edu.vn/api/health/build
./scripts/uat-ux-acceptance-report.sh
```

UAT trình duyệt:

1. Kiểm tra `/teacher-management`, `/student-management`, `/jobs`, `/audit` giữ filter/page/density sau F5.
2. Kiểm tra 360px, 768px, 1366px; chỉ container bảng scroll ngang.
3. Kiểm tra loading, empty, API error/retry và permission denied.
4. Kiểm tra StatusBadge có icon + text.
5. Xuất Audit CSV và đối chiếu filter/RBAC.
6. Mở `/ops/readiness`, kiểm tra panel `UAT UX acceptance`.
