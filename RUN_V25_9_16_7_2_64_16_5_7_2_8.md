# Deploy v25.9.16.7.2.64.16.5.7.2.8 — Batch 35.3.1

Batch này không có migration.

```bash
cd /opt/ai-server

tmux new -s batch35-3-1

rm -rf /tmp/batch35-3-1-deploy
mkdir -p /tmp/batch35-3-1-deploy
unzip -q /tmp/ai-server-openedx-v25.9.16.7.2.64.16.5.7.2.8-student-route-static-export-hotfix-batch35.3.1.zip -d /tmp/batch35-3-1-deploy
PATCH_ROOT=/tmp/batch35-3-1-deploy/ai_server_openedx_v25_9_16_7_2_64_16_5_7_2_8
cp -a "$PATCH_ROOT"/. /opt/ai-server/

sed -i 's/^APP_VERSION=.*/APP_VERSION=25.9.16.7.2.64.16.5.7.2.8/' .env.production

docker compose --env-file .env.production -f docker-compose.prod.yml config >/tmp/batch35-3-1-compose.yml

docker compose --env-file .env.production -f docker-compose.prod.yml build frontend backend \
  2>&1 | tee /tmp/batch35-3-1-build.log

docker compose --env-file .env.production -f docker-compose.prod.yml up -d --no-deps --force-recreate frontend backend
```

Không chạy Alembic, không reset database và không xóa volume.
