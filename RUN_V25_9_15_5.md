# Run v25.9.15.5

## Build

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production build --no-cache backend worker frontend
```

## Migration

Bản này không thêm migration mới, nhưng vẫn chạy Alembic để đảm bảo DB ở head:

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production run --rm backend alembic upgrade head
```

## Up

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend worker frontend
```

## Test nhanh

```bash
curl -i http://localhost:8000/api/health
```

## Test UI

1. Vào `/bank`.
2. Tạo Bộ môn → Môn → Phiên bản môn → Bài.
3. Upload tài liệu, generate/approve câu hỏi.
4. Tạo Release và publish Open edX Library.
5. Vào `/bank/quiz`.
6. Validate course mapping.
7. Validate chapter mapping.
8. Bấm `Xem kế hoạch`.
9. Bấm `Tạo Quiz thật trên Open edX`.
10. Mở Studio kiểm tra Quiz và Problem Bank Beta.
