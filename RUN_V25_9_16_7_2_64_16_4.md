# RUN — v25.9.16.7.2.64.16.4

## Artifact

```text
ai-server-openedx-v25.9.16.7.2.64.16.4-operations-catalog-settings-rbac-ux.zip
root: ai_server_openedx_v25_9_16_7_2_64_16_4
```

## Boundary

- Không có Alembic migration mới; head vẫn là `0052_v25_9_16_7_2_27_learning_behavior_logic_calibration.py`.
- Không reset DB, không xóa Docker volume, không sửa tay `alembic_version`.
- Không thay API contract, Celery semantics, Open edX publish/enrollment hoặc backend RBAC.

## Deploy

```bash
cd /opt/ai-server

rm -rf /tmp/ai-server-v25.9.16.7.2.64.16.4
mkdir -p /tmp/ai-server-v25.9.16.7.2.64.16.4

unzip -q ai-server-openedx-v25.9.16.7.2.64.16.4-operations-catalog-settings-rbac-ux.zip \
  -d /tmp/ai-server-v25.9.16.7.2.64.16.4

rsync -a --delete \
  /tmp/ai-server-v25.9.16.7.2.64.16.4/ai_server_openedx_v25_9_16_7_2_64_16_4/ \
  /opt/ai-server/
```

Cập nhật `.env.production`:

```env
APP_VERSION=25.9.16.7.2.64.16.4
NEXT_PUBLIC_APP_VERSION=25.9.16.7.2.64.16.4
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_ENABLE_DIAGNOSTICS_UI=false
```

Build và recreate:

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production \
  build backend frontend worker beat

docker compose -f docker-compose.prod.yml --env-file .env.production \
  up -d --force-recreate backend frontend worker beat
```

## Verify

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production ps

docker compose -f docker-compose.prod.yml --env-file .env.production \
  exec backend alembic -c alembic.ini current

docker compose -f docker-compose.prod.yml --env-file .env.production \
  exec backend python -c "from app.core.config import settings; print(settings.app_version)"
```

Kết quả version:

```text
25.9.16.7.2.64.16.4
```

Browser UAT bằng role thật:

```text
/jobs
/audit
/ap-sync
/premises
/semesters
/settings
/users
```

Kiểm tra tối thiểu tại 1440px, 1024px, 768px và 390px; xác minh keyboard drawer/tab, URL state, permission denied, loading/error/empty state và thao tác destructive.

## Rollback

Rollback source về `.64.16.3`. Không rollback database vì `.64.16.4` không có migration mới.
