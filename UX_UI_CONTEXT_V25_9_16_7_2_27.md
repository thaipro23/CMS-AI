# UX/UI context v25.9.16.7.2.27

- UI không đổi flow chính; logic đánh giá thay đổi ở backend.
- `/analytics/learning` vẫn là `Môn → Lớp → Xem kết quả`.
- UI tiếp tục dùng nhãn mềm:
  - Có dấu hiệu học thật
  - Có khả năng treo máy
  - Dấu hiệu bất thường cần kiểm tra
  - Chưa đủ dữ liệu
  - Chưa thấy bất thường rõ
- Internal label mới là `POSSIBLE_ANOMALY`; UI không dùng từ `gian lận` hoặc `cheating`.
- Reason/evidence được xem khi bấm vào kết quả sinh viên, không phơi toàn bộ ở bảng chính.
